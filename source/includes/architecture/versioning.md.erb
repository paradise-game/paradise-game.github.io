## Versioning

The API is versioned using the URI of the request. The current version is `v1`. The version is specified in the URI as follows:

> URI Versioning

```
<%= config[:base_url] %>
```

The versioning of the API is based on the [Semantic Versioning](http://semver.org/) specification.