## Read All Notifications

> Request

```http
PATCH <%= config[:version] %>/notification/read/all HTTP/1.1
Host: <%= config[:host] %>
Authorization: Bearer {access_token}
```

```shell
curl --location --request PATCH '<%= config[:base_url] %>/notification/read/all \
--header 'Authorization: Bearer {access_token}' \
```

```javascript
const axios = require('axios');

let config = {
  method: 'patch',
  maxBodyLength: Infinity,
  url: '<%= config[:base_url] %>/notification/read/all',
  headers: { 
    'Authorization': 'Bearer {access_token}'
  }
};

axios.request(config)
  .then((response) => {
    console.log(JSON.stringify(response.data));
  })
  .catch((error) => {
    console.log(error);
  });
```

> Response

```json
{
  "success": true
}
```

사용자에게 발송된 전체 푸시 알림의 읽음 여부를 변경합니다.

읽음 여부 변경을 실행한 후, 그에 대한 성공 여부를 반환합니다.

### HTTP Request

`PATCH <%= config[:version] %>/notification/read/all`

### Scopes

- `user:anonymous`

### Response Code

| Code | Description |
| ---- | ----------- |
| 200  | OK          |

### Response Data

| Parameter | Type     | Description |
| --------- | -------- | ----------- |
| success | `boolean` | 성공 여부 |

### Error Code

| Code | Message | Description |
| ---- | ------- | ----------- |
| 401 | `Unauthorized` | 미 로그인 상태 |